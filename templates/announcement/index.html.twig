{% extends 'base.html.twig' %}

{% block title %}FluffyRagdolls - Announces{% endblock %}

{% block body %}
<body>
    <main>
        <div class="col">
            <div class="col-md-10">
                    <div class="row">
                        <div class="card bg-light mb-4 custom-shadow">
                            <div class="card-body">
                                {% for announcement in announcements %}
                                    <h5>{{ announcement.cat_name }}</h5>
                                    <p class="card-text">{{ announcement.description }}</p>
                                    <p class="card-text">{{ announcement.cat_birth }}</p>
                                {% endfor %}
                            </div>
                        </div>
                    </div>
            </div>
        <br>
    </main>

    <footer>
        <p>© Agathe Dupuis 2024</p>
    </footer>
</body>
{% endblock %}
